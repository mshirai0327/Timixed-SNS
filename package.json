{
  "name": "timixed-diary",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "corepack pnpm --filter @timixed-diary/api dev",
    "dev:web": "corepack pnpm --filter @timixed-diary/web dev",
    "build": "corepack pnpm -r --if-present build",
    "typecheck": "corepack pnpm -r --if-present typecheck"
  }
}
